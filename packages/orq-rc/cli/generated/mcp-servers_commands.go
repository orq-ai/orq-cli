// Code generated by bartolo. DO NOT EDIT.
// See https://github.com/orq-ai/bartolo

package generated

import (
	bartolocli "github.com/orq-ai/bartolo/cli"
	"github.com/pkg/errors"
	"github.com/spf13/cobra"
	"github.com/spf13/viper"
)

func registermcpServersCommands(root *cobra.Command) {
	mcpServersCmd := &cobra.Command{
		Use:   "mcp-servers",
		Short: "Mcp Servers",
		Long:  bartolocli.Markdown("Mcp Servers"),
	}
	root.AddCommand(mcpServersCmd)

	func() {
		parent := mcpServersCmd

		params := viper.New()

		var examples string

		examples += "  " + parent.CommandPath() + " create --example\n"

		cmd := &cobra.Command{
			Use:     "create",
			Short:   "Create an MCP server",
			Long:    bartolocli.Markdown("Creates a new upstream MCP server connection in the workspace.\n\nRequest body: `application/json`. Provide it via stdin or CLI shorthand.\nRun `help-input` for body syntax details.\n\nTop-level fields:\n- `auth` (allOf, required)\n- `connection` (allOf, required)\n- `default_tool_exposure` (allOf, required)\n- `description` (string)\n- `display_name` (string, required)\n- `key` (string, required)\n- `sharing` (allOf)\n\nRequired fields: `auth`, `connection`, `default_tool_exposure`, `display_name`, `key`\n\nAll top-level body fields are exposed as flags for this command. Scalar, nullable scalar (pass `null` for JSON null), enum, repeatable list (`--field a --field b`), and string map (`--field key=value`) fields use typed flags. Nested objects, arrays of objects, and polymorphic unions accept a JSON string (e.g. `--field '{\"k\":1}'`)."),
			Example: examples,
			Args:    cobra.MinimumNArgs(0),
			RunE: func(cmd *cobra.Command, args []string) error {

				bartolocli.MarkPassedFlags(cmd, params)
				if bartolocli.PrintBodyExample(params, "{\n  \"auth\": {\n    \"type\": \"MCP_AUTH_TYPE_UNSPECIFIED\"\n  },\n  \"connection\": {\n    \"type\": \"MCP_CONNECTION_TYPE_UNSPECIFIED\",\n    \"url\": \"url\"\n  },\n  \"default_tool_exposure\": {\n    \"mode\": \"MCP_TOOL_EXPOSURE_MODE_UNSPECIFIED\"\n  },\n  \"display_name\": \"display_name\",\n  \"key\": \"key\"\n}") {
					return nil
				}
				body, err := bartolocli.GetBodyWithFlags(cmd, "application/json", args[0:], params,
					[]bartolocli.BodyField{
						{
							Name:        "auth",
							FlagName:    "auth",
							Type:        "json",
							Description: "Credentials the gateway sends upstream; send `type: NONE` explicitly for public servers.",
						},
						{
							Name:        "connection",
							FlagName:    "connection",
							Type:        "json",
							Description: "How the gateway dials the upstream server.",
						},
						{
							Name:        "default_tool_exposure",
							FlagName:    "default-tool-exposure",
							Type:        "json",
							Description: "Fallback exposure used by any gateway link that does not set its own.",
						},
						{
							Name:        "description",
							FlagName:    "description",
							Type:        "string",
							Description: "Free-form note about what this server is for.",
						},
						{
							Name:        "display_name",
							FlagName:    "display-name",
							Type:        "string",
							Description: "Human readable name shown in the workspace.",
						},
						{
							Name:        "key",
							FlagName:    "key",
							Type:        "string",
							Description: "Lowercase slug of letters, digits, hyphens and underscores, max 64 characters, unique per workspace; prefixes this server's tool names in gateways.",
						},
						{
							Name:        "sharing",
							FlagName:    "sharing",
							Type:        "json",
							Description: "Which projects in the workspace may use this server. Defaults to every project.",
						},
					},
				)
				if err != nil {
					return errors.Wrap(err, "unable to get body")
				}

				_, decoded, err := OpenapiMcpServerCreate(params, body)
				if err != nil {
					return bartolocli.OperationError(err)
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					return errors.Wrap(err, "formatting failed")
				}

				return nil

			},
		}
		parent.AddCommand(cmd)
		bartolocli.AddBodyFlags(cmd)
		bartolocli.AddExampleFlag(cmd)
		bartolocli.AddBodyFieldFlags(cmd,
			[]bartolocli.BodyField{
				{
					Name:        "auth",
					FlagName:    "auth",
					Type:        "json",
					Description: "Credentials the gateway sends upstream; send `type: NONE` explicitly for public servers.",
				},
				{
					Name:        "connection",
					FlagName:    "connection",
					Type:        "json",
					Description: "How the gateway dials the upstream server.",
				},
				{
					Name:        "default_tool_exposure",
					FlagName:    "default-tool-exposure",
					Type:        "json",
					Description: "Fallback exposure used by any gateway link that does not set its own.",
				},
				{
					Name:        "description",
					FlagName:    "description",
					Type:        "string",
					Description: "Free-form note about what this server is for.",
				},
				{
					Name:        "display_name",
					FlagName:    "display-name",
					Type:        "string",
					Description: "Human readable name shown in the workspace.",
				},
				{
					Name:        "key",
					FlagName:    "key",
					Type:        "string",
					Description: "Lowercase slug of letters, digits, hyphens and underscores, max 64 characters, unique per workspace; prefixes this server's tool names in gateways.",
				},
				{
					Name:        "sharing",
					FlagName:    "sharing",
					Type:        "json",
					Description: "Which projects in the workspace may use this server. Defaults to every project.",
				},
			},
		)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		parent := mcpServersCmd

		params := viper.New()

		var examples string

		cmd := &cobra.Command{
			Use:     "delete id",
			Short:   "Delete an MCP server",
			Long:    bartolocli.Markdown("Deletes an MCP server from the workspace. The response body is empty when the delete succeeds.\n\n## Arguments\n\n- `id` — Unique identifier of the MCP server."),
			Example: examples,
			Args:    cobra.MinimumNArgs(1),
			RunE: func(cmd *cobra.Command, args []string) error {

				bartolocli.MarkPassedFlags(cmd, params)
				if err := bartolocli.ConfirmDestructive(cmd, args); err != nil {
					return err
				}

				_, decoded, err := OpenapiMcpServerDelete(args[0], params)
				if err != nil {
					return bartolocli.OperationError(err)
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					return errors.Wrap(err, "formatting failed")
				}

				return nil

			},
		}
		parent.AddCommand(cmd)
		bartolocli.AddForceFlag(cmd)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		parent := mcpServersCmd

		params := viper.New()

		var examples string

		cmd := &cobra.Command{
			Use:     "list",
			Short:   "List MCP servers",
			Long:    bartolocli.Markdown("Returns a paginated list of MCP servers in the workspace."),
			Example: examples,
			Args:    cobra.MinimumNArgs(0),
			RunE: func(cmd *cobra.Command, args []string) error {

				bartolocli.MarkPassedFlags(cmd, params)

				_, decoded, err := OpenapiMcpServerList(params)
				if err != nil {
					return bartolocli.OperationError(err)
				}

				if err := bartolocli.FormatList(decoded); err != nil {
					return errors.Wrap(err, "formatting failed")
				}

				return nil

			},
		}
		parent.AddCommand(cmd)

		cmd.Flags().Int64("limit", 0, "Page size between 1 and 200. Defaults to 25.")
		cmd.Flags().String("starting-after", "", "Cursor for the page after the given item id. Mutually exclusive with `ending_before`.")
		cmd.Flags().String("ending-before", "", "Cursor for the page before the given item id. Mutually exclusive with `starting_after`.")
		cmd.Flags().String("search", "", "Case-insensitive match against the server key and display name.")

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		parent := mcpServersCmd

		params := viper.New()

		var examples string

		cmd := &cobra.Command{
			Use:     "retrieve id",
			Short:   "Retrieve an MCP server",
			Long:    bartolocli.Markdown("Retrieves the details of an existing MCP server by its unique ID.\n\n## Arguments\n\n- `id` — Unique identifier of the MCP server."),
			Example: examples,
			Args:    cobra.MinimumNArgs(1),
			RunE: func(cmd *cobra.Command, args []string) error {

				bartolocli.MarkPassedFlags(cmd, params)

				_, decoded, err := OpenapiMcpServerGet(args[0], params)
				if err != nil {
					return bartolocli.OperationError(err)
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					return errors.Wrap(err, "formatting failed")
				}

				return nil

			},
		}
		parent.AddCommand(cmd)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		parent := mcpServersCmd

		params := viper.New()

		var examples string

		examples += "  " + parent.CommandPath() + " sync id --example\n"

		cmd := &cobra.Command{
			Use:     "sync id",
			Short:   "Sync an MCP server",
			Long:    bartolocli.Markdown("Connects to an upstream MCP server, discovers tools, and persists the tool list and sync state on the server record.\n\nRequest body: `application/json`. Provide it via stdin or CLI shorthand.\nRun `help-input` for body syntax details.\n\nTop-level fields:\n- `discovery_variables` (object)\n\nAll top-level body fields are exposed as flags for this command. Scalar, nullable scalar (pass `null` for JSON null), enum, repeatable list (`--field a --field b`), and string map (`--field key=value`) fields use typed flags. Nested objects, arrays of objects, and polymorphic unions accept a JSON string (e.g. `--field '{\"k\":1}'`).\n\n## Arguments\n\n- `id` — Unique identifier of the MCP server."),
			Example: examples,
			Args:    cobra.MinimumNArgs(1),
			RunE: func(cmd *cobra.Command, args []string) error {

				bartolocli.MarkPassedFlags(cmd, params)
				if bartolocli.PrintBodyExample(params, "{\n  \"discovery_variables\": {}\n}") {
					return nil
				}
				body, err := bartolocli.GetBodyWithFlags(cmd, "application/json", args[1:], params,
					[]bartolocli.BodyField{
						{
							Name:        "discovery_variables",
							FlagName:    "discovery-variables",
							Type:        "string-map",
							Description: "Values for the server's `template_variables`; treated as sensitive and not persisted.",
						},
					},
				)
				if err != nil {
					return errors.Wrap(err, "unable to get body")
				}

				_, decoded, err := OpenapiMcpServerSync(args[0], params, body)
				if err != nil {
					return bartolocli.OperationError(err)
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					return errors.Wrap(err, "formatting failed")
				}

				return nil

			},
		}
		parent.AddCommand(cmd)
		bartolocli.AddBodyFlags(cmd)
		bartolocli.AddExampleFlag(cmd)
		bartolocli.AddBodyFieldFlags(cmd,
			[]bartolocli.BodyField{
				{
					Name:        "discovery_variables",
					FlagName:    "discovery-variables",
					Type:        "string-map",
					Description: "Values for the server's `template_variables`; treated as sensitive and not persisted.",
				},
			},
		)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		parent := mcpServersCmd

		params := viper.New()

		var examples string

		examples += "  " + parent.CommandPath() + " test-tool id --example\n"

		cmd := &cobra.Command{
			Use:     "test-tool id",
			Short:   "Test an MCP server tool",
			Long:    bartolocli.Markdown("Executes a single tool on an upstream MCP server for testing. Connects to the server, invokes the tool with the provided arguments, and returns the result.\n\nRequest body: `application/json`. Provide it via stdin or CLI shorthand.\nRun `help-input` for body syntax details.\n\nTop-level fields:\n- `arguments` (object)\n- `discovery_variables` (object)\n- `tool_name` (string, required)\n\nRequired fields: `tool_name`\n\nAll top-level body fields are exposed as flags for this command. Scalar, nullable scalar (pass `null` for JSON null), enum, repeatable list (`--field a --field b`), and string map (`--field key=value`) fields use typed flags. Nested objects, arrays of objects, and polymorphic unions accept a JSON string (e.g. `--field '{\"k\":1}'`).\n\n## Arguments\n\n- `id` — Unique identifier of the MCP server."),
			Example: examples,
			Args:    cobra.MinimumNArgs(1),
			RunE: func(cmd *cobra.Command, args []string) error {

				bartolocli.MarkPassedFlags(cmd, params)
				if bartolocli.PrintBodyExample(params, "{\n  \"tool_name\": \"tool_name\"\n}") {
					return nil
				}
				body, err := bartolocli.GetBodyWithFlags(cmd, "application/json", args[1:], params,
					[]bartolocli.BodyField{
						{
							Name:        "arguments",
							FlagName:    "arguments",
							Type:        "json",
							Description: "Arguments passed to the tool, matching its `input_schema`.",
						},
						{
							Name:        "discovery_variables",
							FlagName:    "discovery-variables",
							Type:        "string-map",
							Description: "Values for the server's `template_variables`; treated as sensitive and not persisted.",
						},
						{
							Name:        "tool_name",
							FlagName:    "tool-name",
							Type:        "string",
							Description: "Bare upstream tool name, not a gateway's namespaced `exposed_name`.",
						},
					},
				)
				if err != nil {
					return errors.Wrap(err, "unable to get body")
				}

				_, decoded, err := OpenapiMcpServerTestTool(args[0], params, body)
				if err != nil {
					return bartolocli.OperationError(err)
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					return errors.Wrap(err, "formatting failed")
				}

				return nil

			},
		}
		parent.AddCommand(cmd)
		bartolocli.AddBodyFlags(cmd)
		bartolocli.AddExampleFlag(cmd)
		bartolocli.AddBodyFieldFlags(cmd,
			[]bartolocli.BodyField{
				{
					Name:        "arguments",
					FlagName:    "arguments",
					Type:        "json",
					Description: "Arguments passed to the tool, matching its `input_schema`.",
				},
				{
					Name:        "discovery_variables",
					FlagName:    "discovery-variables",
					Type:        "string-map",
					Description: "Values for the server's `template_variables`; treated as sensitive and not persisted.",
				},
				{
					Name:        "tool_name",
					FlagName:    "tool-name",
					Type:        "string",
					Description: "Bare upstream tool name, not a gateway's namespaced `exposed_name`.",
				},
			},
		)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		parent := mcpServersCmd

		params := viper.New()

		var examples string

		examples += "  " + parent.CommandPath() + " update id --example\n"

		cmd := &cobra.Command{
			Use:     "update id",
			Short:   "Update an MCP server",
			Long:    bartolocli.Markdown("Updates mutable fields of an existing MCP server. Omitted optional fields keep their current values.\n\nRequest body: `application/json`. Provide it via stdin or CLI shorthand.\nRun `help-input` for body syntax details.\n\nTop-level fields:\n- `auth` (allOf)\n- `connection` (allOf)\n- `default_tool_exposure` (allOf)\n- `description` (string)\n- `display_name` (string)\n- `key` (string)\n- `sharing` (allOf)\n\nAll top-level body fields are exposed as flags for this command. Scalar, nullable scalar (pass `null` for JSON null), enum, repeatable list (`--field a --field b`), and string map (`--field key=value`) fields use typed flags. Nested objects, arrays of objects, and polymorphic unions accept a JSON string (e.g. `--field '{\"k\":1}'`).\n\n## Arguments\n\n- `id` — Unique identifier of the MCP server."),
			Example: examples,
			Args:    cobra.MinimumNArgs(1),
			RunE: func(cmd *cobra.Command, args []string) error {

				bartolocli.MarkPassedFlags(cmd, params)
				if bartolocli.PrintBodyExample(params, "{\n  \"auth\": {\n    \"type\": \"MCP_AUTH_TYPE_UNSPECIFIED\"\n  },\n  \"connection\": {\n    \"type\": \"MCP_CONNECTION_TYPE_UNSPECIFIED\",\n    \"url\": \"url\"\n  },\n  \"default_tool_exposure\": {\n    \"mode\": \"MCP_TOOL_EXPOSURE_MODE_UNSPECIFIED\"\n  },\n  \"description\": \"description\",\n  \"display_name\": \"display_name\",\n  \"key\": \"key\",\n  \"sharing\": {\n    \"all_projects\": {},\n    \"allow_fork\": false,\n    \"allow_version_pin\": false,\n    \"auto_grant_new_projects\": false,\n    \"selected\": {\n      \"project_ids\": [\n        \"project_ids\"\n      ]\n    }\n  }\n}") {
					return nil
				}
				body, err := bartolocli.GetBodyWithFlags(cmd, "application/json", args[1:], params,
					[]bartolocli.BodyField{
						{
							Name:        "auth",
							FlagName:    "auth",
							Type:        "json",
							Description: "Credentials the gateway sends upstream; send `type: NONE` explicitly for public servers.",
						},
						{
							Name:        "connection",
							FlagName:    "connection",
							Type:        "json",
							Description: "How the gateway dials the upstream server.",
						},
						{
							Name:        "default_tool_exposure",
							FlagName:    "default-tool-exposure",
							Type:        "json",
							Description: "Fallback exposure used by any gateway link that does not set its own.",
						},
						{
							Name:        "description",
							FlagName:    "description",
							Type:        "string",
							Description: "Free-form note about what this server is for.",
						},
						{
							Name:        "display_name",
							FlagName:    "display-name",
							Type:        "string",
							Description: "Human readable name shown in the workspace.",
						},
						{
							Name:        "key",
							FlagName:    "key",
							Type:        "string",
							Description: "Rejected with INVALID_ARGUMENT: the key prefixes this server's tools in\n every gateway that exposes it, so it is immutable after creation. Retained\n so callers get an error rather than a silently ignored field.",
						},
						{
							Name:        "sharing",
							FlagName:    "sharing",
							Type:        "json",
							Description: "Which projects in the workspace may use this server. Defaults to every project.",
						},
					},
				)
				if err != nil {
					return errors.Wrap(err, "unable to get body")
				}

				_, decoded, err := OpenapiMcpServerUpdate(args[0], params, body)
				if err != nil {
					return bartolocli.OperationError(err)
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					return errors.Wrap(err, "formatting failed")
				}

				return nil

			},
		}
		parent.AddCommand(cmd)
		bartolocli.AddBodyFlags(cmd)
		bartolocli.AddExampleFlag(cmd)
		bartolocli.AddBodyFieldFlags(cmd,
			[]bartolocli.BodyField{
				{
					Name:        "auth",
					FlagName:    "auth",
					Type:        "json",
					Description: "Credentials the gateway sends upstream; send `type: NONE` explicitly for public servers.",
				},
				{
					Name:        "connection",
					FlagName:    "connection",
					Type:        "json",
					Description: "How the gateway dials the upstream server.",
				},
				{
					Name:        "default_tool_exposure",
					FlagName:    "default-tool-exposure",
					Type:        "json",
					Description: "Fallback exposure used by any gateway link that does not set its own.",
				},
				{
					Name:        "description",
					FlagName:    "description",
					Type:        "string",
					Description: "Free-form note about what this server is for.",
				},
				{
					Name:        "display_name",
					FlagName:    "display-name",
					Type:        "string",
					Description: "Human readable name shown in the workspace.",
				},
				{
					Name:        "key",
					FlagName:    "key",
					Type:        "string",
					Description: "Rejected with INVALID_ARGUMENT: the key prefixes this server's tools in\n every gateway that exposes it, so it is immutable after creation. Retained\n so callers get an error rather than a silently ignored field.",
				},
				{
					Name:        "sharing",
					FlagName:    "sharing",
					Type:        "json",
					Description: "Which projects in the workspace may use this server. Defaults to every project.",
				},
			},
		)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

}
