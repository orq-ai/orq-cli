// Code generated by bartolo. DO NOT EDIT.
// See https://github.com/orq-ai/bartolo

package generated

import (
	bartolocli "github.com/orq-ai/bartolo/cli"
	"github.com/rs/zerolog/log"
	"github.com/spf13/cobra"
	"github.com/spf13/viper"
)

func registeractivitiesCommands(root *cobra.Command) {
	activitiesCmd := &cobra.Command{
		Use:   "activities",
		Short: "Activities",
		Long:  bartolocli.Markdown("Activities form the timeline of an entity. The timeline mixes user-authored comments and version snapshots that record how the entity changed over time."),
	}
	root.AddCommand(activitiesCmd)

	func() {
		params := viper.New()

		var examples string

		examples += "  " + activitiesCmd.CommandPath() + " create entity-id --example\n"

		cmd := &cobra.Command{
			Use:     "create entity-id",
			Short:   "Create a new activity",
			Long:    bartolocli.Markdown("Appends an activity to the timeline of an entity. Comment activities require `content` and may mention users or reply to another comment. Version activities require `checksum`, `version`, and `data`, and snapshot the entity configuration at that point in time. The target entity must exist and be accessible to the API key; the activity is attached to the project of that entity, with workspace and project scope inferred from the key.\n\nRequest body: `application/json`. Provide it via stdin or CLI shorthand.\nRun `help-input` for body syntax details.\n\nTop-level fields:\n- `checksum` (string)\n- `content` (string)\n- `data` (object)\n- `description` (string)\n- `entity_type` (string, required)\n- `mentions` (array)\n- `parent_activity_id` (string)\n- `project_id` (string)\n- ... and 2 more fields\n\nRequired fields: `entity_type`, `type`\n\nAll top-level body fields are exposed as flags for this command. Scalar, nullable scalar (pass `null` for JSON null), enum, repeatable list (`--field a --field b`), and string map (`--field key=value`) fields use typed flags. Nested objects, arrays of objects, and polymorphic unions accept a JSON string (e.g. `--field '{\"k\":1}'`)."),
			Example: examples,
			Args:    cobra.MinimumNArgs(1),
			Run: func(cmd *cobra.Command, args []string) {
				if bartolocli.PrintBodyExample(params, "{\n  \"entity_type\": \"entity_type\",\n  \"type\": \"ACTIVITY_TYPE_UNSPECIFIED\"\n}") {
					return
				}
				body, err := bartolocli.GetBodyWithFlags(cmd, "application/json", args[1:], params,
					[]bartolocli.BodyField{
						{
							Name:        "checksum",
							FlagName:    "checksum",
							Type:        "string",
							Description: "Content hash of the snapshotted entity configuration. Required for\n version activities, ignored for comment activities.",
						},
						{
							Name:        "content",
							FlagName:    "content",
							Type:        "string",
							Description: "Comment body. Required for comment activities, ignored for version\n activities.",
						},
						{
							Name:        "data",
							FlagName:    "data",
							Type:        "json",
							Description: "Snapshot of the entity configuration. Required for version\n activities, ignored for comment activities.",
						},
						{
							Name:        "description",
							FlagName:    "description",
							Type:        "string",
							Description: "Human-readable summary of what changed in this version. Only used\n for version activities.",
						},
						{
							Name:        "entity_type",
							FlagName:    "entity-type",
							Type:        "string",
							Description: "Type of the entity the activity belongs to. One of `agent`,\n `dataset`, `evaluator`, `experiment_v2`, `knowledge`,\n `memory_store`, `prompt_library`, `prompt_snippet`, `sheet`,\n `skill`, or `tool`.",
						},
						{
							Name:        "mentions",
							FlagName:    "mentions",
							Type:        "json",
							Description: "Users mentioned in the comment body. Only used for comment\n activities.",
						},
						{
							Name:        "parent_activity_id",
							FlagName:    "parent-activity-id",
							Type:        "string",
							Description: "ID of the comment this comment replies to. Leave empty for\n top-level comments. Only used for comment activities.",
						},
						{
							Name:        "project_id",
							FlagName:    "project-id",
							Type:        "string",
							Description: "Project that contains the entity. Optional; the server resolves the\n project from the entity, and when this field is provided it must\n match that project.",
						},
						{
							Name:        "type",
							FlagName:    "type",
							Type:        "enum-string",
							Description: "Kind of activity to create. `ACTIVITY_TYPE_UNSPECIFIED` is\n rejected.",
							Enum: []string{
								"ACTIVITY_TYPE_UNSPECIFIED",
								"ACTIVITY_TYPE_COMMENT",
								"ACTIVITY_TYPE_VERSION",
							},
						},
						{
							Name:        "version",
							FlagName:    "version",
							Type:        "string",
							Description: "Semantic version to assign to the snapshot, for example `1.4.0`.\n Required for version activities, ignored for comment activities.",
						},
					},
				)
				if err != nil {
					log.Fatal().Err(err).Msg("unable to get body")
				}

				_, decoded, err := OpenapiActivityCreate(args[0], params, body)
				if err != nil {
					log.Fatal().Err(err).Msg("error calling operation")
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					log.Fatal().Err(err).Msg("formatting failed")
				}

			},
		}
		activitiesCmd.AddCommand(cmd)
		bartolocli.AddBodyFlags(cmd)
		bartolocli.AddExampleFlag(cmd)
		bartolocli.AddBodyFieldFlags(cmd,
			[]bartolocli.BodyField{
				{
					Name:        "checksum",
					FlagName:    "checksum",
					Type:        "string",
					Description: "Content hash of the snapshotted entity configuration. Required for\n version activities, ignored for comment activities.",
				},
				{
					Name:        "content",
					FlagName:    "content",
					Type:        "string",
					Description: "Comment body. Required for comment activities, ignored for version\n activities.",
				},
				{
					Name:        "data",
					FlagName:    "data",
					Type:        "json",
					Description: "Snapshot of the entity configuration. Required for version\n activities, ignored for comment activities.",
				},
				{
					Name:        "description",
					FlagName:    "description",
					Type:        "string",
					Description: "Human-readable summary of what changed in this version. Only used\n for version activities.",
				},
				{
					Name:        "entity_type",
					FlagName:    "entity-type",
					Type:        "string",
					Description: "Type of the entity the activity belongs to. One of `agent`,\n `dataset`, `evaluator`, `experiment_v2`, `knowledge`,\n `memory_store`, `prompt_library`, `prompt_snippet`, `sheet`,\n `skill`, or `tool`.",
				},
				{
					Name:        "mentions",
					FlagName:    "mentions",
					Type:        "json",
					Description: "Users mentioned in the comment body. Only used for comment\n activities.",
				},
				{
					Name:        "parent_activity_id",
					FlagName:    "parent-activity-id",
					Type:        "string",
					Description: "ID of the comment this comment replies to. Leave empty for\n top-level comments. Only used for comment activities.",
				},
				{
					Name:        "project_id",
					FlagName:    "project-id",
					Type:        "string",
					Description: "Project that contains the entity. Optional; the server resolves the\n project from the entity, and when this field is provided it must\n match that project.",
				},
				{
					Name:        "type",
					FlagName:    "type",
					Type:        "enum-string",
					Description: "Kind of activity to create. `ACTIVITY_TYPE_UNSPECIFIED` is\n rejected.",
					Enum: []string{
						"ACTIVITY_TYPE_UNSPECIFIED",
						"ACTIVITY_TYPE_COMMENT",
						"ACTIVITY_TYPE_VERSION",
					},
				},
				{
					Name:        "version",
					FlagName:    "version",
					Type:        "string",
					Description: "Semantic version to assign to the snapshot, for example `1.4.0`.\n Required for version activities, ignored for comment activities.",
				},
			},
		)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		params := viper.New()

		var examples string

		cmd := &cobra.Command{
			Use:     "list entity-id",
			Short:   "List activities for an entity",
			Long:    bartolocli.Markdown("Returns the activity timeline of an entity, ordered by creation time with the newest entry first. The timeline mixes `comment` and `version` activities. Use `starting_after` or `ending_before` to page through large collections; the two cursors are mutually exclusive."),
			Example: examples,
			Args:    cobra.MinimumNArgs(1),
			Run: func(cmd *cobra.Command, args []string) {

				_, decoded, err := OpenapiActivityList(args[0], params)
				if err != nil {
					log.Fatal().Err(err).Msg("error calling operation")
				}

				if err := bartolocli.FormatList(decoded); err != nil {
					log.Fatal().Err(err).Msg("formatting failed")
				}

			},
		}
		activitiesCmd.AddCommand(cmd)

		cmd.Flags().Int64("limit", 0, "Page size, 1–200. Unset uses the server default (25); explicit 0\n (or anything outside the range) is rejected by buf.validate.")
		cmd.Flags().String("starting-after", "", "Cursor for forward pagination. Set to the `activity_id` of the\n last item from the previous page. Mutually exclusive with\n `ending_before`.")
		cmd.Flags().String("ending-before", "", "Cursor for backward pagination. Set to the `activity_id` of the\n first item from the previous page. Mutually exclusive with\n `starting_after`.")

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		params := viper.New()

		var examples string

		examples += "  " + activitiesCmd.CommandPath() + " update entity-id activity-id --example\n"

		cmd := &cobra.Command{
			Use:     "update entity-id activity-id",
			Short:   "Update an activity",
			Long:    bartolocli.Markdown("Updates an existing activity. Set `content` to edit a comment; the comment is marked as edited and the edit time is recorded. Set `environments` to retag a version activity: the listed environments are removed from every other version of the same entity because an environment always points at a single version. Omitted fields keep their current values.\n\nRequest body: `application/json`. Provide it via stdin or CLI shorthand.\nRun `help-input` for body syntax details.\n\nTop-level fields:\n- `content` (string)\n- `environments` (allOf)\n\nAll top-level body fields are exposed as flags for this command. Scalar, nullable scalar (pass `null` for JSON null), enum, repeatable list (`--field a --field b`), and string map (`--field key=value`) fields use typed flags. Nested objects, arrays of objects, and polymorphic unions accept a JSON string (e.g. `--field '{\"k\":1}'`)."),
			Example: examples,
			Args:    cobra.MinimumNArgs(2),
			Run: func(cmd *cobra.Command, args []string) {
				if bartolocli.PrintBodyExample(params, "{\n  \"content\": \"content\",\n  \"environments\": {\n    \"values\": [\n      \"values\"\n    ]\n  }\n}") {
					return
				}
				body, err := bartolocli.GetBodyWithFlags(cmd, "application/json", args[2:], params,
					[]bartolocli.BodyField{
						{
							Name:        "content",
							FlagName:    "content",
							Type:        "string",
							Description: "New comment body. Omit to keep the current content. Setting this\n marks the comment as edited and records the edit time.",
						},
						{
							Name:        "environments",
							FlagName:    "environments",
							Type:        "json",
							Description: "Replacement environment list for a version activity. Omit to keep\n the current environments. The listed environments are removed from\n every other version of the same entity.",
						},
					},
				)
				if err != nil {
					log.Fatal().Err(err).Msg("unable to get body")
				}

				_, decoded, err := OpenapiActivityUpdate(args[0], args[1], params, body)
				if err != nil {
					log.Fatal().Err(err).Msg("error calling operation")
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					log.Fatal().Err(err).Msg("formatting failed")
				}

			},
		}
		activitiesCmd.AddCommand(cmd)
		bartolocli.AddBodyFlags(cmd)
		bartolocli.AddExampleFlag(cmd)
		bartolocli.AddBodyFieldFlags(cmd,
			[]bartolocli.BodyField{
				{
					Name:        "content",
					FlagName:    "content",
					Type:        "string",
					Description: "New comment body. Omit to keep the current content. Setting this\n marks the comment as edited and records the edit time.",
				},
				{
					Name:        "environments",
					FlagName:    "environments",
					Type:        "json",
					Description: "Replacement environment list for a version activity. Omit to keep\n the current environments. The listed environments are removed from\n every other version of the same entity.",
				},
			},
		)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

}
