// Code generated by bartolo. DO NOT EDIT.
// See https://github.com/orq-ai/bartolo

package generated

import (
	bartolocli "github.com/orq-ai/bartolo/cli"
	"github.com/rs/zerolog/log"
	"github.com/spf13/cobra"
	"github.com/spf13/viper"
)

func registerpeopleCommands(root *cobra.Command) {
	peopleCmd := &cobra.Command{
		Use:   "people",
		Short: "People",
		Long:  bartolocli.Markdown("People are workspace members and invited users. Manage roles, groups, and invitations for your team."),
	}
	root.AddCommand(peopleCmd)

	func() {
		params := viper.New()

		var examples string

		examples += "  " + peopleCmd.CommandPath() + " person-create --example\n"

		cmd := &cobra.Command{
			Use:     "person-create",
			Short:   "Invite people to a workspace",
			Long:    bartolocli.Markdown("Invites one or more people to the current workspace. If an email is not already registered, a new account is created and an invitation email is sent. Existing accounts are linked directly to the workspace.\n\nRequest body: `application/json`. Provide it via stdin or CLI shorthand.\nRun `help-input` for body syntax details.\n\nTop-level fields:\n- `emails` (array, required)\n- `groups` (array)\n- `roles` (array)\n\nRequired fields: `emails`\n\nAll top-level body fields are exposed as flags for this command. Scalar, nullable scalar (pass `null` for JSON null), enum, repeatable list (`--field a --field b`), and string map (`--field key=value`) fields use typed flags. Nested objects, arrays of objects, and polymorphic unions accept a JSON string (e.g. `--field '{\"k\":1}'`)."),
			Example: examples,
			Args:    cobra.MinimumNArgs(0),
			Run: func(cmd *cobra.Command, args []string) {
				if bartolocli.PrintBodyExample(params, "{\n  \"emails\": [\n    \"emails\"\n  ]\n}") {
					return
				}
				body, err := bartolocli.GetBodyWithFlags(cmd, "application/json", args[0:], params,
					[]bartolocli.BodyField{
						{
							Name:        "emails",
							FlagName:    "emails",
							Type:        "string-slice",
							Description: "Email addresses to invite. At least one email is required.",
						},
						{
							Name:        "groups",
							FlagName:    "groups",
							Type:        "string-slice",
							Description: "Group IDs to assign.",
						},
						{
							Name:        "roles",
							FlagName:    "roles",
							Type:        "string-slice",
							Description: "Roles to assign. Defaults to [\"member\"] when empty.",
						},
					},
				)
				if err != nil {
					log.Fatal().Err(err).Msg("unable to get body")
				}

				_, decoded, err := OpenapiPersonCreate(params, body)
				if err != nil {
					log.Fatal().Err(err).Msg("error calling operation")
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					log.Fatal().Err(err).Msg("formatting failed")
				}

			},
		}
		peopleCmd.AddCommand(cmd)
		bartolocli.AddBodyFlags(cmd)
		bartolocli.AddExampleFlag(cmd)
		bartolocli.AddBodyFieldFlags(cmd,
			[]bartolocli.BodyField{
				{
					Name:        "emails",
					FlagName:    "emails",
					Type:        "string-slice",
					Description: "Email addresses to invite. At least one email is required.",
				},
				{
					Name:        "groups",
					FlagName:    "groups",
					Type:        "string-slice",
					Description: "Group IDs to assign.",
				},
				{
					Name:        "roles",
					FlagName:    "roles",
					Type:        "string-slice",
					Description: "Roles to assign. Defaults to [\"member\"] when empty.",
				},
			},
		)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		params := viper.New()

		var examples string

		cmd := &cobra.Command{
			Use:     "person-delete person-id",
			Short:   "Delete a person",
			Long:    bartolocli.Markdown("Removes a member or invited user from the workspace. API keys owned by the removed user are cascade revoked. The response body is empty when the delete succeeds."),
			Example: examples,
			Args:    cobra.MinimumNArgs(1),
			Run: func(cmd *cobra.Command, args []string) {

				_, decoded, err := OpenapiPersonDelete(args[0], params)
				if err != nil {
					log.Fatal().Err(err).Msg("error calling operation")
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					log.Fatal().Err(err).Msg("formatting failed")
				}

			},
		}
		peopleCmd.AddCommand(cmd)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		params := viper.New()

		var examples string

		cmd := &cobra.Command{
			Use:     "person-get person-id",
			Short:   "Retrieve a person",
			Long:    bartolocli.Markdown("Retrieves the details of an existing workspace member or invited user by their person ID."),
			Example: examples,
			Args:    cobra.MinimumNArgs(1),
			Run: func(cmd *cobra.Command, args []string) {

				_, decoded, err := OpenapiPersonGet(args[0], params)
				if err != nil {
					log.Fatal().Err(err).Msg("error calling operation")
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					log.Fatal().Err(err).Msg("formatting failed")
				}

			},
		}
		peopleCmd.AddCommand(cmd)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		params := viper.New()

		var examples string

		cmd := &cobra.Command{
			Use:     "person-list",
			Short:   "List all people",
			Long:    bartolocli.Markdown("Returns a paginated list of people in the current workspace. Use `starting_after` or `ending_before` to page through large collections."),
			Example: examples,
			Args:    cobra.MinimumNArgs(0),
			Run: func(cmd *cobra.Command, args []string) {

				_, decoded, err := OpenapiPersonList(params)
				if err != nil {
					log.Fatal().Err(err).Msg("error calling operation")
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					log.Fatal().Err(err).Msg("formatting failed")
				}

			},
		}
		peopleCmd.AddCommand(cmd)

		cmd.Flags().Int64("limit", 0, "Page size, 1-200. Unset uses the server default.")
		cmd.Flags().String("starting-after", "", "Cursor for forward pagination. Set to the `id` of the last\n item from the previous page.")
		cmd.Flags().String("ending-before", "", "Cursor for backward pagination. Set to the `id` of the first\n item from the previous page.")

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		params := viper.New()

		var examples string

		cmd := &cobra.Command{
			Use:     "person-resend-invitation person-id",
			Short:   "Resend invitation",
			Long:    bartolocli.Markdown("Resends the invitation email to a pending workspace member. Has no effect if the person has already accepted.\n\nRequest body: `application/json`. Provide it via stdin or CLI shorthand.\nRun `help-input` for body syntax details.\n\nTop-level type: `object`"),
			Example: examples,
			Args:    cobra.MinimumNArgs(1),
			Run: func(cmd *cobra.Command, args []string) {
				body, err := bartolocli.GetBodyWithFlags(cmd, "application/json", args[1:], params,
					[]bartolocli.BodyField{},
				)
				if err != nil {
					log.Fatal().Err(err).Msg("unable to get body")
				}

				_, decoded, err := OpenapiPersonResendInvitation(args[0], params, body)
				if err != nil {
					log.Fatal().Err(err).Msg("error calling operation")
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					log.Fatal().Err(err).Msg("formatting failed")
				}

			},
		}
		peopleCmd.AddCommand(cmd)
		bartolocli.AddBodyFlags(cmd)
		bartolocli.AddBodyFieldFlags(cmd,
			[]bartolocli.BodyField{},
		)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

	func() {
		params := viper.New()

		var examples string

		examples += "  " + peopleCmd.CommandPath() + " person-update person-id --example\n"

		cmd := &cobra.Command{
			Use:     "person-update person-id",
			Short:   "Update a person",
			Long:    bartolocli.Markdown("Updates the roles and group assignments of a workspace member. Omitted fields keep their current values.\n\nRequest body: `application/json`. Provide it via stdin or CLI shorthand.\nRun `help-input` for body syntax details.\n\nTop-level fields:\n- `clear_groups` (boolean)\n- `clear_roles` (boolean)\n- `groups` (array)\n- `roles` (array)\n\nAll top-level body fields are exposed as flags for this command. Scalar, nullable scalar (pass `null` for JSON null), enum, repeatable list (`--field a --field b`), and string map (`--field key=value`) fields use typed flags. Nested objects, arrays of objects, and polymorphic unions accept a JSON string (e.g. `--field '{\"k\":1}'`)."),
			Example: examples,
			Args:    cobra.MinimumNArgs(1),
			Run: func(cmd *cobra.Command, args []string) {
				if bartolocli.PrintBodyExample(params, "{\n  \"clear_groups\": false,\n  \"clear_roles\": false,\n  \"groups\": [\n    \"groups\"\n  ],\n  \"roles\": [\n    \"roles\"\n  ]\n}") {
					return
				}
				body, err := bartolocli.GetBodyWithFlags(cmd, "application/json", args[1:], params,
					[]bartolocli.BodyField{
						{
							Name:        "clear_groups",
							FlagName:    "clear-groups",
							Type:        "bool",
							Description: "Explicitly clear all groups. Set to true when sending an empty groups array.",
						},
						{
							Name:        "clear_roles",
							FlagName:    "clear-roles",
							Type:        "bool",
							Description: "Explicitly clear all roles. Set to true when sending an empty roles array.",
						},
						{
							Name:        "groups",
							FlagName:    "groups",
							Type:        "string-slice",
							Description: "Replacement group list. Leave empty to clear all groups.",
						},
						{
							Name:        "roles",
							FlagName:    "roles",
							Type:        "string-slice",
							Description: "Replacement role list. Leave empty to clear all roles.",
						},
					},
				)
				if err != nil {
					log.Fatal().Err(err).Msg("unable to get body")
				}

				_, decoded, err := OpenapiPersonUpdate(args[0], params, body)
				if err != nil {
					log.Fatal().Err(err).Msg("error calling operation")
				}

				if err := bartolocli.Formatter.Format(decoded); err != nil {
					log.Fatal().Err(err).Msg("formatting failed")
				}

			},
		}
		peopleCmd.AddCommand(cmd)
		bartolocli.AddBodyFlags(cmd)
		bartolocli.AddExampleFlag(cmd)
		bartolocli.AddBodyFieldFlags(cmd,
			[]bartolocli.BodyField{
				{
					Name:        "clear_groups",
					FlagName:    "clear-groups",
					Type:        "bool",
					Description: "Explicitly clear all groups. Set to true when sending an empty groups array.",
				},
				{
					Name:        "clear_roles",
					FlagName:    "clear-roles",
					Type:        "bool",
					Description: "Explicitly clear all roles. Set to true when sending an empty roles array.",
				},
				{
					Name:        "groups",
					FlagName:    "groups",
					Type:        "string-slice",
					Description: "Replacement group list. Leave empty to clear all groups.",
				},
				{
					Name:        "roles",
					FlagName:    "roles",
					Type:        "string-slice",
					Description: "Replacement role list. Leave empty to clear all roles.",
				},
			},
		)

		bartolocli.SetCustomFlags(cmd)

		if cmd.Flags().HasFlags() {
			params.BindPFlags(cmd.Flags())
		}

	}()

}
